`std::common_type` kisa tekrar.

```cpp
template<typename T>
struct TypeIdentity {
    using type = T;
};

template<typename T>
using TypeIdentity_t = typename TypeIdentity<T>::type;

int main()
{
    TypeIdentity<int>::type x;
}
```

@00:14

```cpp
#include <type_traits>
#include <vector>

template<typename T>
void foo(std::vector<T>& vec, T x);

int main()
{
    std::vector<int> ivec;

    foo(ivec, 12);
    // foo(ivec, 'A'); Error.
}
```

Yukaridaki kodda 2. parametreye `'A'` argumanini yollamak deduction asamasinda conflict'e sebep oluyor. Fonksiyona gonderilen 2. arguman icin deduction hic yapilmasin seklinde bir amacimiz varsa, `foo(ivec, 'A')` cagrisinin legal olmasi isteniyorsa; 

- Explicit template argument kullanilabilir. `foo<int>(ivec, 'A')`
- `std::type_identity_t` kullanilabilir:

```cpp
#include <type_traits>
#include <vector>

template<typename T>
void foo(std::vector<T>& vec, std::type_identity_t<T> x);

int main()
{
    std::vector<int> ivec;

    foo(ivec, 12);
    foo(ivec, 'A');
}
```

> C++ standardına göre, bir template parametresi typename Some<T>::type formunda bir nested type'ın içinde göründüğünde, o parametre non-deduced context'e girer. Yani compiler o parametreyi o argümandan çıkarmaya çalışmaz. (bkz: [*non-deduced context*](https://stackoverflow.com/questions/25245453/what-is-a-non-deduced-context))

### Fold Expression Tricks

@00:28

Jonathan Muller'in teknigi:

```cpp
#include <iostream>

void f(int i)
{
    std::cout << i << ' ';
}

template<typename ...Ts>
void reverse_call(Ts ...ts)
{
    int dummy;
    (dummy = ... = (f(ts), 0));
}

int main()
{
    reverse_call(1, 2, 3, 4, 5);
}

/* 
    Out:
    5 4 3 2 1 
*/
```

Yukaridaki teknigi daha iyi anlamak icin C++17 ile dile eklenen *order of evaluation* garantisi; atama operatorunun sag operandi olan ifadenin daha once yapilmasi ve onun yan etkilerinin tamamlanmasindan sonra, sol operand olan ifadenin ele alinmasi.

```cpp
#include <iostream>

void f()
{
    printf("necati\n");
}

void g()
{
    printf("ergin\n");
}

int main()
{
    int x;

    (x =  (f(), 0)) = (g(), 0);
}

/* 
    Out:
    ergin
    necati
*/
```

---

```cpp
#include <iostream>

bool pred(int i)
{
    return i % 2 == 0;
}

template <typename ... Ts>
std::size_t count(Ts... ts)
{
    return (std::size_t(0) + ... + (pred(ts) ? 1 : 0));
    // 0 + ( (pred(p1) ? 1 : 0) + (pred(p2) ? 1 : 0) ) + (pred(p3) ? 1 : 0)
}

int main()
{
    std::cout << count(4, 1, 6, 3, 8, 10);
}

/* 
    Out:
    4
*/
```

---

@00:43

```cpp
#include <iostream>

template <typename ...Ts>
auto get_last(Ts ...args)
{
    return (args, ...);
}

int main()
{
    std::cout << get_last(1, 3, 7);
}

/* 
    Out:
    7
*/
```

```cpp
#include <iostream>
#include <type_traits>

template <typename ...Ts>
std::common_type_t<Ts...> get_last(Ts ...args)
{
    return (args, ...);
}

int main()
{
    std::cout << get_last(1.7, 3, 7);
}

/* 
    Out:
    7
*/
```

Yukaridaki kodda, fonksiyonun geri donus degeri turu ilk argumanin turu.

```cpp
#include <iostream>
#include <type_traits>

template <typename ...Ts>
std::common_type_t<Ts...> get_first(Ts ...args)
{
    // std::common_type_t<Ts...> result;
    std::common_type_t<decltype(args)...> result;

    ( (result = args, true) || ...);
    return result;
}

int main()
{
    std::cout << get_first(1.7, 3, 7);
}

/* 
    Out:
    1.7
*/
```

```cpp
#include <iostream>

template<typename First, typename ...Args>
void fold_print(First&& f, Args&&... args)
{
    std::cout << f;
    auto print_with_comma = [](const auto& v) {
        std::cout << ", " << v;
        };

    (..., print_with_comma(std::forward<Args>(args)));

    std::cout << '\n';
}

int main()
{
    fold_print("necati", 10, 20, 30, "ergin");
}

/* 
    Out:
    necati, 10, 20, 30, ergin
*/
```

```cpp
#include <vector>
#include <iostream>

template <typename T, typename ...TS>
decltype(auto) push_back(std::vector<T>& vec, TS&& ...args)
{
	(vec.push_back(std::forward<TS>(args)), ...);
	return (vec);
}

int main()
{
	std::vector<int> ivec;

	push_back(ivec, 1, 2, 3, 4).push_back(5);
	
	for (auto i : ivec)
		std::cout << i << ' ';
}

/* 
    Out:
    1 2 3 4 5
*/
```

@00:59

## *CRTP Idiom* ~ *Curiously Recurring Template Pattern*

```cpp
#include <iostream>

template <typename Der>
class Base {

public:
	void print_type() const
	{
		std::cout << typeid(*this).name() << '\n';
		std::cout << typeid(Der).name() << '\n';
		std::cout << "--------------------------------------\n";
	}
};

class A : public Base<A> {};
class B : public Base<B> {};
class C : public Base<C> {};


int main()
{
	A ax;
	B bx;
	C cx;

	ax.print_type();
	bx.print_type();
	cx.print_type();
}

/* 
    Out:
    class Base<class A>
    class A
    --------------------------------------
    class Base<class B>
    class B
    --------------------------------------
    class Base<class C>
    class C
    --------------------------------------
*/
```

```cpp
#include <iostream>

template <typename Der>
class Base {
public:
    void func()
    {
        static_cast<Der*>(this)->foo();
        static_cast<Der&>(*this).foo();
    }
};

class Der : public Base<Der> {
public:
    void foo()
    {
        std::cout << "Der::foo()\n";
    }
};


int main()
{
    Der myder;

    myder.func();
}

/* 
    Out:
    Der::foo()
    Der::foo()
*/
```

Virtual Dispatch mekanizmasi hic yok, compile time'da Virtual Dispatch etkisi yapiyor.

```cpp
#include <iostream>

template <typename Der>
class Base {
public:
    void func()
    {
        static_cast<Der*>(this)->foo();
        // static_cast<Der&>(*this).foo();
    }
};

class Der : public Base<Der> {
public:
    void foo()
    {
        std::cout << "Der::foo()\n";
    }
};

class Mer : public Base<Mer> {
public:
    void foo()
    {
        std::cout << "Mer::foo()\n";
    }
};

class Fer : public Base<Fer> {
public:
    void foo()
    {
        std::cout << "Fer::foo()\n";
    }
};

int main()
{
    Der myDer;
    Mer myMer;
    Fer myFer;

    myDer.func();
    myMer.func();
    myFer.func();
}

/* 
    Out:
    Der::foo()
    Mer::foo()
    Fer::foo()
*/
```

Virtual Dispatch'e bir alternatif. Fakat hicbir farkli teknik birbirinin es degeri degil!

Static Polymorphism de denir.

Taban sinifin verdigi interface illa turemis sinifin uye fonksiyonlarini kullanmak zorunda degil.

Kullanildigi yerler:
- Taban sinifin turemis sinifa bir interface ve implementasyon vermesi.
- ...

```cpp
#include <cstddef>

template <typename T>
class Counter {
public:
	Counter()
	{
		++ms_count_created;
		++ms_count_alive;
	}

	Counter(const Counter&)
	{
		++ms_count_created;
		++ms_count_alive;
	}

	~Counter()
	{
		--ms_count_alive;
	}

	static std::size_t get_created_count() { return ms_count_created; }
	static std::size_t get_alive_count() { return ms_count_alive; }
private:
	inline static std::size_t ms_count_created{};
	inline static std::size_t ms_count_alive{};
};



class Nec : Counter<Nec> {
public:
	using Counter<Nec>::get_alive_count;
	using Counter<Nec>::get_created_count;
};

class Erg : Counter<Erg> {
public:
	using Counter<Erg>::get_alive_count;
	using Counter<Erg>::get_created_count;
};

#include <iostream>

int main()
{
	Nec n1, n2, n3;
	{
		Nec n1, n2, n3;
	}
	std::cout << Nec::get_alive_count() << '\n';
	std::cout << Nec::get_created_count() << '\n';

	Erg e1, e2;
	{
		Erg e1, e2, e3, e4;
	}
	std::cout << Erg::get_alive_count() << '\n';
	std::cout << Erg::get_created_count() << '\n';
}


/* 
    Out:
    3
    6
    2
    6
*/
```

```cpp
//another alternative to CRTP

#include <cstddef>

// template <auto = []{}>
template <typename = decltype([]{})>
class Counter {
public:
	Counter()
	{
		++ms_count_created;
		++ms_count_alive;
	}

	Counter(const Counter&)
	{
		++ms_count_created;
		++ms_count_alive;
	}

	~Counter()
	{
		--ms_count_alive;
	}

	static std::size_t get_created_count() { return ms_count_created; }
	static std::size_t get_alive_count() { return ms_count_alive; }
private:
	inline static std::size_t ms_count_created{};
	inline static std::size_t ms_count_alive{};
};



class Nec : public Counter<> {
public:
};

class Erg : public Counter<> {
public:
};

#include <iostream>

int main()
{
	Nec n1, n2, n3;
	{
		Nec n1, n2, n3;
	}
	std::cout << Nec::get_alive_count() << '\n';
	std::cout << Nec::get_created_count() << '\n';

	Erg e1, e2;
	{
		Erg e1, e2, e3, e4;
	}
	std::cout << Erg::get_alive_count() << '\n';
	std::cout << Erg::get_created_count() << '\n';
}


/* 
    Out:
    3
    6
    2
    6
*/
```
Yukaridaki kodda template parametresi default template argumani aliyor. Ayni lambda ifadesi tekrar ettiginde derleyici farkli bir closure type olusturmak zorunda. Fakat bu CRTP degil ve C++20 gerektiriyor. Bu yontem farkli taban sinif olusmasini sagliyor fakat CRTP degil. CRTP'de tek amac bu degil, daha cok taban sinifin uye fonksiyonunun taban sinifin uye fonksiyonunu cagirmasini saglamak.

```cpp
#include <type_traits>

template <typename = decltype([]{})>
class Myclass {};

int main()
{
    Myclass<> x;
    Myclass<> y;

    static_assert(not std::is_same_v<decltype(x), decltype(y)>);
}
```

@01:29

### *CRTP*, *Virtual Dispatch*'e Ne Sekilde Alternatif Olur?

```cpp
#include <iostream>

template <class Derived>
struct Base
{
    void interface()
    {
        // burada kod olabilir
        static_cast<Derived*>(this)->implementation();
        // burada kod olabilir
    }

    static void static_interface()
    {
        // burada kod olabilir
        Derived::static_implementation();
        // burada kod olabilir
    }

    void implementation()
    {
        std::cout << "Base implementation\n";
    }

    static void static_implementation()
    {
        std::cout << "Base static_implementation\n";
    }
};

struct ADer : Base<ADer>
{
    void implementation() // <1>
    {
        std::cout << "ADer implementation\n";
    }

    static void static_implementation() // <1>
    {
        std::cout << "ADer static_implementation\n";
    }
};

int main()
{
    ADer a;

    a.interface();
    ADer::static_interface();
}

/* 
    Out<!1>: 
    Base implementation
    Base static_implementation

    Out<1>:
    ADer implementation
    ADer static_implementation
*/
```

Bunun kullanildigi birkac tipik tema var:
- Birden fazla sinifa ortak kod yazmak yerine, kod tekrarini engellemek icin CRTP ile bu kodu taban sinifa gommek.
- ...

```cpp
#include <type_traits>

template<typename T, typename = void>
struct has_less : std::false_type{};

template <typename T>
struct has_less<T, std::void_t<decltype(std::declval<T>() < std::declval<T>())>> : std::true_type {};

template<typename T>
bool constexpr has_less_v = has_less<T>::value;

template <typename Der>
struct comparison {
    
    const Der& derived()const
	{
        static_assert(has_less_v<Der>, "Derived class must provide operator <");
		return static_cast<const Der&>(*this);
	}

	friend bool operator>(const comparison<Der>& left, const comparison<Der>& right)
	{
		return right.derived() < left.derived();
	};

	friend bool operator>=(const comparison<Der>& left, const comparison<Der>& right)
	{
		return !(left.derived() < right.derived());
	}

	friend bool operator<=(const comparison<Der>& left, const comparison<Der>& right)
	{
		return !(right.derived() < left.derived());
	};

	friend bool operator==(const comparison<Der>& left, const comparison<Der>& right)
	{
		return !(left.derived() < right.derived()) && !(right.derived() < left.derived());
	}


	friend bool operator!=(const comparison<Der>& left, const comparison<Der>& right)
	{
		return !(left == right);
	}
};

class Nec : public comparison<Nec> {
public:
	Nec(int x) : x_{ x } {}
	bool operator<(const Nec& other)const
	{
		return x_ < other.x_;
	}
private:
	int x_;
};

#include <string>

class Person : public comparison<Person> {
public:
	Person(std::string name) : name_(std::move(name)) {}
	bool operator<(const Person& other)const
	{
		return name_ < other.name_;
	}
private:
	std::string name_;
};

#include <iostream>


int main()
{
	using namespace std;

	Nec x{ 132 }, y{ 43 };
	cout << boolalpha;

	cout << "x < y  : " << (x < y) << '\n';
	cout << "x <= y : " << (x <= y) << '\n';
	cout << "x > y  : " << (x > y) << '\n';
	cout << "x >= y : " << (x >= y) << '\n';
	cout << "x == y : " << (x == y) << '\n';
	cout << "x != y : " << (x != y) << '\n';

	Person p1{ "necati" };
	Person p2{ "necati" };

	cout << "p1 < p2  : " << (p1 < p2) << '\n';
	cout << "p1 <= p2 : " << (p1 <= p2) << '\n';
	cout << "p1 > p2  : " << (p1 > p2) << '\n';
	cout << "p1 >= p2 : " << (p1 >= p2) << '\n';
	cout << "p1 == p2 : " << (p1 == p2) << '\n';
	cout << "p1 != p2 : " << (p1 != p2) << '\n';
}

/* 
    Out:
    x < y  : false
    x <= y : false
    x > y  : true
    x >= y : true
    x == y : false
    x != y : true
    p1 < p2  : false
    p1 <= p2 : true
    p1 > p2  : false
    p1 >= p2 : true
    p1 == p2 : true
    p1 != p2 : false
*/
```

```cpp
#include <string>
#include <iostream>

template<typename Printable>
struct Print_NTimes
{
    void print_n(std::size_t n) const
    {
        while (n-- > 0)
        {
            static_cast<const Printable &>(*this).print();
            //static_cast<const Printable *>(this)->print();
        }
    }
};

class Name : public Print_NTimes<Name>
{
public:
    Name() = default;
    Name(std::string first_name, std::string last_name)
        : m_first_name(std::move(first_name))
        , m_last_name(std::move(last_name)) {}

    void print() const
    {
        std::cout << m_first_name << ' ' << m_last_name << '\n';
    }

private:
    std::string m_first_name;
    std::string m_last_name;
};

int main()
{
    Name nec("Necati", "Ergin");
    nec.print_n(10);
}

/* 
    Out:
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
    Necati Ergin
*/
```

@01:54

```cpp
//CRTP base singleton class

#include <memory>
#include <mutex>

template<typename TDerived>
class Singleton
{
    inline static std::unique_ptr<TDerived> m_instance{};
    inline static std::once_flag            m_once{};
protected:
    Singleton() {}
public:
    ~Singleton() { }
    Singleton(const Singleton&) = delete;
    Singleton& operator=(const Singleton&) = delete;
    static TDerived& GetInstance()
    {
        std::call_once
        (
            Singleton::m_once,
            []() { Singleton::m_instance.reset(new TDerived()); }
        );
        return *m_instance;
    }
};


#include <iostream>

class Myclass : public Singleton<Myclass>
{
public:
    Myclass() 
    {
        std::cout << "Myclass ctor\n";
    }

    ~Myclass() 
    {
        std::cout << "Myclass destructor\n";
    }

    void foo()
    {
        std::cout << "Myclass::foo()\n";
    }
};

int main()
{
    Myclass::GetInstance().foo();
}

/* 
    Out:
    Myclass ctor
    Myclass::foo()
    Myclass destructor
*/
```

```cpp
#include <iterator>
#include <cassert>
#include <iostream>

template <typename Der>
class Container {
private:
    Der& derived() { return static_cast<Der&>(*this); }
    const Der& derived() const { return static_cast<const Der&>(*this); }
public:
    decltype(auto) front()
    {
        return *derived().begin();
    }

    decltype(auto) back()
    {
        return *std::prev(derived().end());
    }

    decltype(auto) size() const
    {
        return std::distance(derived().begin(), derived().end());
    }

    decltype(auto) operator[](std::size_t i)
    {
        return *std::next(derived().begin(), i);
    }
};

#include <memory>

template <typename T>
class DynArray : public Container<DynArray<T>> {
public:
    DynArray(std::size_t size) : size_{ size },
        updata_{ std::make_unique<T[]>(size_) }
    { }

    T* begin() { return updata_.get(); }
    const T* begin() const { return updata_.get(); }
    T* end() { return updata_.get() + size_; }
    const T* end() const { return updata_.get() + size_; }
private:
    std::size_t size_;
    std::unique_ptr<T[]> updata_;
};

int main()
{
    DynArray<int> arr(10);
    arr.front() = 2;
    arr[2] = 5;
    arr.back() = 67;

    for (auto val : arr)
        std::cout << val << ' ';

    std::cout << '\n';
    std::cout << "arr.size() = " << arr.size() << '\n';
}

/* 
    Out:
    2 0 5 0 0 0 0 0 0 67 
    arr.size() = 10
*/
```

```cpp
#include <ostream>

class Printer
{
public:
    Printer(std::ostream& pstream) : m_stream(pstream) {}

    template <typename T>
    Printer& print(const T& t)
    {
        m_stream << t;
        return *this;
    }

    template <typename T>
    Printer& print_line(const T& t)
    {
        m_stream << t << '\n';
        return *this;
    }
private:
    std::ostream& m_stream;
};


#include <iostream>

int main()
{
    Printer(std::cout).print("necati ").print_line("ergin");
}

/* 
    Out:
    necati ergin
*/
```

```cpp
#include <ostream>

class Printer
{
public:
    Printer(std::ostream& pstream) : m_stream(pstream) {}

    template <typename T>
    Printer& print(const T& t)
    {
        m_stream << t << ' ';
        return *this;
    }

    template <typename T>
    Printer& print_line(const T& t)
    {
        m_stream << t << '\n';
        return *this;
    }
private:
    std::ostream& m_stream;
};

#include <iostream>


class ConsolePrinter : public Printer
{
public:
    ConsolePrinter() : Printer(std::cout) {}

    ConsolePrinter& set_color(int color)
    {
        std::cout << "console color set\n";
        return *this;
    }
};

int main()
{
    ConsolePrinter().print("Necati ").set_color(12).print_line("Ergin"); // invalid
}
```

```cpp
#include <ostream>

template <typename ConcretePrinter>
class Printer
{
public:
    Printer(std::ostream& pstream) : m_stream(pstream) {}

    template <typename T>
    ConcretePrinter& print(const T& t)
    {
        m_stream << t;
        return static_cast<ConcretePrinter&>(*this);
    }

    template <typename T>
    ConcretePrinter& print_line(const T &t)
    {
        m_stream << t << '\n';
        return static_cast<ConcretePrinter&>(*this);
    }
private:
    std::ostream& m_stream;
};

#include <iostream>

class ConsolePrinter : public Printer<ConsolePrinter>
{
public:
    ConsolePrinter() : Printer(std::cout) {}

    ConsolePrinter& set_color(int c)
    {
        return *this;
    }
};

int main()
{
    ConsolePrinter().print("Necati").set_color(15).print_line("Ergin");
}

/* 
    Out:
    NecatiErgin
*/
```

```cpp
#include <iostream>

template <typename Der>
class A {
public:
	void foo()
	{
		static_cast<Der*>(this)->f1();
	}
};

template <typename Der>
class B {
public:
	void bar()
	{
		static_cast<Der*>(this)->f2();
	}
};

class Myclass : public A<Myclass>, public B<Myclass> {
public:
	void f1()
	{
		std::cout << "Myclass::f1()\n";
	}

	void f2()
	{
		std::cout << "Myclass::f2()\n";
	}
};

int main()
{
	Myclass mx;

	mx.bar();
	mx.foo();
}

/* 
    Out:
    Myclass::f2()
    Myclass::f1()
*/
```

```cpp
#include <iostream>

template<typename T>
class Animal {
public:
	void cry()
	{
		static_cast<T*>(this)->make_sound();
	}
};

class Dog : public Animal<Dog> {
public:
	void make_sound()
	{
		std::cout << "hav hav hav!!!\n";
	}
};

class Cat : public Animal<Cat> {
public:
	void make_sound()
	{
		std::cout << "miyav miyav miyav!!!\n";
	}
};

class Lamb : public Animal<Lamb> {
public:
	void make_sound()
	{
		std::cout << "meeeeeeeeeee!!!\n";
	}
};

template<typename T>
void game(Animal<T>& x)
{
	x.cry();
}

int main()
{
	Dog karabas;
	Cat minnos;
	Lamb kuzucuk;

	/*karabas.cry();
	minnos.cry();
	kuzucuk.cry();*/

	game(karabas);
	game(minnos);
	game(kuzucuk);
}

/* 
    Out:
    hav hav hav!!!
    miyav miyav miyav!!!
    meeeeeeeeeee!!!
*/
```