### Hidden Friend Idiom (Tekrar)

>  - Yalnizca ADL ile erisilebiliyor.
>  - Cogunlukla operator overloading icin kullaniliyor.

00:39
## *Enum Turleri Ile Ilgili Idiomatic Yapilar*

> Eskiden olan enumlar; *unscoped enum*
> Yeni gelen enumlar; *scoped enum* (enum class, enum struct)


```cpp
#include <cstdint>
#include <iostream>
#include <type_traits>

enum class Fruit : uint8_t
{
    Apple,
    Banana,
    Orange,
    Grape,
    Mango,
};

int main()
{
    Fruit f{ Fruit::Orange};
    using und_type = std::underlying_type_t<Fruit>;

    // Buradaki operator +, integral promotion olmamasi icin.
    std::cout << "Enum value: " << +static_cast<und_type>(f) << '\n';
}
```

@01:21
### *Strong Bool Idiom*

```cpp
#define STRONG_BOOL(e)   \
    enum class e : bool  \
    {                    \
        No  = false,    \
        Yes = true      \
    }

STRONG_BOOL(Open);
STRONG_BOOL(Close);
STRONG_BOOL(ReadOnly);
STRONG_BOOL(Write);

void access_file(Open open, Close close, ReadOnly readonly, Write write);

void foo()
{
    access_file(Open::Yes, Close::No, ReadOnly::No, Write::No);
}
```

> Bilerek isteyerek *unscoped enum* kullanmak soz konusu olabilir mi? Aksi yonde karar
> almamizi gerektirecek bir durum yoksa *scoped enum* kullan. Hala *unscoped enum* 
> kullanmaya gerek duyulacak birkac senaryo var:
>
> - Bitsel islemler.
> - Yazdigim kodun C'de de gecerli olmasini istiyorsam.

Enum turlerinin ozellikle *scoped enum* turlerinin bit-mask olarak kullanilmasini secime 
birakacak ve bunu generic hale getirecek basit bir modul yazalim.

```cpp


```